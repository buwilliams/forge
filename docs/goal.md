# Goal

Here's the goal: produce a plugin which better than my current workflow. I typically
write a design.md file then ask Claude Code to make an implementation plan our of it (generates a
bunch of tasks), then I put them in a ralph-loop until it is finished. My current process has the
problem that it doesn't follow instructions well since it will often ignore things like "no stubs,
mocks, or smoke-tests" and "use real data", its like that gets lose in the context, aka. context rot.
 Does this plugin really improve the siutation?
